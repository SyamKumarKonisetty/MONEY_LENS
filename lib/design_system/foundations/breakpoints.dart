/// Viewport breakpoint tokens for MoneyLens Design System (MLDS).
///
/// Prepares the system for future responsive layout scaling.
class MLBreakpoints {
  MLBreakpoints._();

  static const double phone = 360.0;
  static const double foldable = 600.0;
  static const double tablet = 768.0;
  static const double desktop = 1024.0;
}

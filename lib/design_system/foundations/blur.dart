/// Backdrop filter blur tokens for MoneyLens Design System (MLDS).
///
/// Controls the blur sigma values for premium semi-transparent surfaces.
class MLBlur {
  MLBlur._();

  static const double card = 10.0;
  static const double navigation = 15.0;
  static const double sheet = 20.0;
  static const double dialog = 25.0;
}

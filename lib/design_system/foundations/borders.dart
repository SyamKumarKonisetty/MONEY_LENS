/// Border width tokens for MoneyLens Design System (MLDS).
///
/// Controls consistent hairline cuts, active states, and selection weights.
class MLBorders {
  MLBorders._();

  static const double hairline = 0.5;
  static const double thin = 1.0;
  static const double medium = 2.0;
  static const double focus = 2.0;
  static const double selected = 1.5;
  static const double error = 1.5;
  static const double disabled = 1.0;
}

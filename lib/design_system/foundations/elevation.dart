/// Elevation tokens for MoneyLens Design System (MLDS).
///
/// Controls the relative visual height (z-index) of layers.
class MLElevation {
  MLElevation._();

  static const double flat = 0.0;
  static const double raised = 1.0;
  static const double floating = 4.0;
  static const double overlay = 8.0;
  static const double modal = 12.0;
  static const double bottomSheet = 16.0;
}
